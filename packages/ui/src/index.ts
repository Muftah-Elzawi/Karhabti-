/**
 * Shared Tamagui components (web + native).
 *
 * Components arrive with the web skeleton (Phase 1, Step 6); until then this
 * package only re-exports the design tokens so apps have a single source of
 * truth for brand values.
 */
export {
  palette,
  fontFamilies,
  fontWeights,
  typeScale,
  space,
  radius,
} from '@karhabti/config/tamagui/tokens';
