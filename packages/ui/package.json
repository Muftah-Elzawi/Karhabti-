{
  "name": "@karhabti/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Tamagui components (web + native). Tamagui wiring lands with the web skeleton (Phase 1, Step 6).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx,.cjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@karhabti/config": "workspace:*"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "typescript": "^5.5.4"
  }
}
