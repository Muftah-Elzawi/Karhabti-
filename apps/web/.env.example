# Karhabti web — copy to .env.local for local development. Never commit real env files.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Session cookies are Secure (HTTPS-only) in production builds. Set to false
# ONLY for plain-HTTP smoke tests of `next start` on localhost.
# COOKIE_SECURE=false
