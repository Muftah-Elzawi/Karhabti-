import { Module } from '@nestjs/common';

/**
 * Root module. The clean-architecture skeleton (domain / application /
 * infrastructure / presentation), error handling, logging, and the health
 * endpoint land in Phase 1, Step 4.
 */
@Module({})
export class AppModule {}
